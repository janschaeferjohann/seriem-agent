# Anthropic API Key (required)
ANTHROPIC_API_KEY=

# Storage path for agent workspace (relative to backend folder)
STORAGE_PATH=../storage

# Model configuration (optional)
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# =============================================================================
# Telemetry Configuration
# =============================================================================

# Enable local telemetry collection (default: enabled)
# Set to 0 to disable
TELEMETRY_ENABLED=1

# Directory for local telemetry JSONL files (default: backend/telemetry_data)
# TELEMETRY_DIR=./telemetry_data

# =============================================================================
# LangSmith Configuration (Backend-only, for development/ops)
# =============================================================================
# LangSmith provides deep tracing of LLM calls and tool usage.
# This is a developer/ops setting, not exposed to end users.
# Get your API key at https://smith.langchain.com

# Enable LangSmith tracing (default: disabled)
LANGSMITH_TRACING=false

# LangSmith API key (required if LANGSMITH_TRACING=true)
LANGSMITH_API_KEY=

# LangSmith endpoint (default: US - https://api.smith.langchain.com)
# For EU region, use: https://eu.api.smith.langchain.com
LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# LangSmith project name (organizes traces, default: "default")
LANGSMITH_PROJECT=seriem-agent

