<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns="urn:kwsoft:mtext:tonic:simpledom" targetNamespace="urn:kwsoft:mtext:tonic:simpledom" elementFormDefault="qualified">
	
	<!-- Enum -->	
	<xs:simpleType name="RegionProduceOnLastPageEnumType">
	 <xs:restriction base="xs:string">
	 	<xs:enumeration value="only">
	 	<xs:annotation><xs:documentation>The region will be produced only, if the current layout is used for the
last document page.</xs:documentation></xs:annotation>
	 	</xs:enumeration>
	 	<xs:enumeration value="avoid">
	 	<xs:annotation><xs:documentation>The region will not be produced, if the current layout is used for the
last document page.</xs:documentation></xs:annotation>
	 	</xs:enumeration>
	 	<xs:enumeration value="default">
	 	<xs:annotation><xs:documentation>The region will be produced, independent from the current layout being
used on the last or any other document page.
This the default value if the property is empty or not set at all.</xs:documentation></xs:annotation>
	 	</xs:enumeration>
  	 </xs:restriction>
	</xs:simpleType>
	<!-- /Enum -->
	
</xs:schema>