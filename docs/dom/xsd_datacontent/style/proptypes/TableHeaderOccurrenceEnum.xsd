<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns="urn:kwsoft:mtext:tonic:simpledom" targetNamespace="urn:kwsoft:mtext:tonic:simpledom" elementFormDefault="qualified">
	
	<!-- Enum -->	
	<xs:simpleType name="TableHeaderOccurrenceEnumType">
	 <xs:restriction base="xs:string">
	 	<xs:enumeration value="all">
	 	<xs:annotation><xs:documentation>Include the header on all pages the table spans.</xs:documentation></xs:annotation>
	 	</xs:enumeration>
	 	<xs:enumeration value="all_but_first">
	 	<xs:annotation><xs:documentation>Include the header on all pages except the first one the table spans.</xs:documentation></xs:annotation>
	 	</xs:enumeration>
	 	<xs:enumeration value="first">
	 	<xs:annotation><xs:documentation>Include the header only on the page the table starts at.</xs:documentation></xs:annotation>
	 	</xs:enumeration>
  	 </xs:restriction>
	</xs:simpleType>
	<!-- /Enum -->
	
</xs:schema>